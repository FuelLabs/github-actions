name: Reusable workflow

on:
  workflow_call:

jobs:
  cargo-toml-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/cargo@v1
        with:
          command: install
          args: cargo-toml-lint
      - run: cargo toml-lint